# Project Evidence

This directory contains selected screenshots and validation evidence for the Ansible Server Automation project.

Planned evidence includes:

- Ansible connectivity (`ping: pong`)
- Base setup idempotency
- Docker installation and service validation
- Docker handler execution
- UFW firewall validation
- Nginx container deployment
- HTTP `200 OK` validation
- Final end-to-end validation
- Successful `ansible-lint` result

Only selected screenshots should be stored here to keep the repository clean and professional.

